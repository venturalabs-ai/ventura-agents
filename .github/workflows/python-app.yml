# DEPRECATED
# This workflow has been replaced by the unified CI pipeline:
# .github/workflows/ci.yml
#
# Keeping this file only to avoid broken links in old PRs.
# It does nothing.

name: Python application (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use ci.yml instead."
